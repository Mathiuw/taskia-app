import { Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"

const HelpScreen = () => {
    return (
        <SafeAreaView>
            <Text>Aba de ajuda aqui!!</Text>
        </SafeAreaView>
    )
}

export default HelpScreen